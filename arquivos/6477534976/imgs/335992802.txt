 

victornevesGatena:-$ python3 -m venv «virtualenvs/vicvenv
The virtual environment was not created successfully because ensurepip
is not

available. On Debian/Ubuntu systems, you need to install the python3-v
[A

package using the following command.

apt install python3.8-venv
You may need to use sudo with that command. After installing the pytho
[oct VícTAA E E E

package, recreate your virtual environment.

Failing command: ['/home/victorneves/.virtualenvs/vicvenv/bin/python3!,
'-Im', 'ensurepip', '--upgrade!, !--default-pip']
